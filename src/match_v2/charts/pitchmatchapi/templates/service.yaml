apiVersion: v1
kind: Service
metadata:
  name: {{ template "pitchmatchapi.fullname" . }}
  labels:
    app: {{ template "pitchmatchapi.name" . }}
    chart: {{ template "pitchmatchapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "pitchmatchapi.name" . }}
    release: {{ .Release.Name }}
