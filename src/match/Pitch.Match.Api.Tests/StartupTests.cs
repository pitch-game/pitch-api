using Microsoft.AspNetCore.Hosting;
using Microsoft.Extensions.DependencyInjection;
using Pitch.Match.API.ApplicationCore.Services;
using Xunit;

namespace Pitch.Match.API.Tests
{
    public class StartupTests
    {
        //[Fact]
        //public void StartupTest()
        //{
        //    var webHost = new WebHostBuilder().UseStartup<Startup>().Build();
        //    Assert.NotNull(webHost);
        //    Assert.NotNull(webHost.Services.GetRequiredService<IMatchService>());
        //    Assert.NotNull(webHost.Services.GetRequiredService<IMatchmakingService>());
        //}
    }
}
