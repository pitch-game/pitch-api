[environments]
  [environments.development]
    name = "match"
    namespace = "default"
    wait = true
    watch = true
    watch-delay = 2
    auto-connect = true
    dockerfile = "Dockerfile"
    chart = "../../k8s/helm/match"