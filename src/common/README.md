When the microservice platform is installed and configured in a microservice, a pipeline is set up that ensures that the microservice can be monitored, that it logs as expected, and that it performs the security checks you’ve decided on.

