version: '3.4'

services:
  pitch.card.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "64729:80"
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
    networks:
      - pitchnetwork
    container_name: card
networks:
  pitchnetwork:
    external: true